import {
  KEY_DOWN,
  KEY_UP,
  register_action
} from '/src/shared.js'

const FORWARD  = 'w'
const BACKWARD = 's'
const LEFT     = 'a'
const RIGHT    = 'd'

}





    })

    register_action(KEY_UP, key, scene, () => {
    })
  }





}

  return (e) => {
  }
}

export {
  process_pointer_move,
  init_controls
}